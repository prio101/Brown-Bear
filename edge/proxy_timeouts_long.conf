# Timeouts for routes that run a model: inference and embedding.
#
# Generating tokens or embedding a large document legitimately takes minutes on
# CPU, so the default 120s would abort work that was going to succeed. Matches
# the app's own BB_OLLAMA_TIMEOUT_SECONDS default of 600.
proxy_send_timeout 600s;
proxy_read_timeout 600s;
